// ============================================
// DATA STRUCTURE & INITIAL DATA
// ============================================

const INITIAL_DATA = `## 2000 | tech
**DeSy**
El área de desarrollo de Domingo Alonso Group crea su primera aplicación, DeSy, una herramienta destinada a facilitar a los importadores los pedidos de vehículos al fabricante VW, AUDI, SKODA.

## 2001 | tech
**Primeros servidores web Linux (DAWEB)**
Implementación de infraestructura web basada en Linux.

## 2001 | product | ⭐
**Aplicación Base**
Domingo Alonso Group demuestra un compromiso sólido con el desarrollo de software al crear su propio sistema ERP, diseñado específicamente para optimizar la gestión de concesionarios e importadores.

## 2002 | tech
**Directorio**
Sistema de directorio corporativo.

## 2003 | tech
**Primera línea contra fábrica**
Gestión de las redes de los CCs externos.

## 2004 | product
**CoFi**
Aplicación de gestión financiera.

## 2004 | product
**Desy arranca en 400 CC en Alemania**
Expansión internacional de DeSy a 400 centros en Alemania.

## 2005 | product | ⭐
**Suite de aplicaciones SIMA**
Se refuerza significativamente la inversión en nuestra factoría de software, proyectando su potencial comercialización en nuevos mercados.

## 2005 | tech
**Footprints (Marzo)**
Sistema de gestión de tickets e incidencias.

## 2005 | tech
**Blackberry para directores**
Primera adopción de dispositivos móviles para directivos.

## 2008 | org | ⭐
**Nacimiento de AIDA**
Fundación de la primera empresa Tech del Grupo.

## 2009 | product | ⭐
**Primer cliente internacional de AIDA**
SIMA se implanta en Luxemburgo.

## 2010 | tech | ⭐
**Primer CPD DAG**
Primer Centro de Datos en Miller Bajo propio desde el que se entregan el 100% de los servicios consumidos por el Grupo.

## 2011 | tech | ⭐
**Nube DAG**
DAG implementó un modelo de trabajo 100% virtual, eliminando la necesidad de ordenadores y portátiles. Este sistema se basa en la entrega de escritorios remotos directamente desde su CPD propio, lo cual supuso una reducción de la inversión en aquel momento y potenció la movilidad de los empleados.

## 2013 | product
**AX**
Implementación de Microsoft Dynamics AX.

## 2014 | product
**Comienzo de los POKs (Point of Kiosks)**
Instalación del primer servicio de kiosk en Canarias para el sector de rent a car. Este sistema permite a los clientes gestionar de forma autónoma la recogida y devolución de vehículos, posicionando a Mobility a la vanguardia en un servicio diferenciador para la experiencia de cliente y el autoservicio en hoteles.

## 2016 | tech | ⭐
**Google Suite**
El Grupo Domingo Alonso migra sus soluciones de herramientas de colaboración a la nube de Google. Con este movimiento, DAG se posiciona como una de las empresas pioneras en el archipiélago en adoptar esta tecnología, reforzando su compromiso con la seguridad, la colaboración en tiempo real y la movilidad.

## 2017 | tech
**Nueva apuesta por la movilidad**
DAG da un paso más en la movilidad, fomentando el uso de portátiles y ofreciendo nuevas soluciones que permitan a sus empleados trabajar desde cualquier lugar. Este hecho benefició particularmente al Grupo en 2020, cuando fuimos confinados a causa de la pandemia.

## 2017 | org | ⭐
**Nace el DTO (Oficina de Transformación Digital)**
Creación de la oficina de transformación digital.

## 2018 | product | ⭐
**Activación de ERP Propio (RMS) para Mobility**
Lanzamiento del sistema de gestión de reservas, flota y revenue de diseño propio. El objetivo principal fue crear una plataforma más moderna para optimizar la gestión integral del negocio de alquiler de vehículos con el objetivo de automatizar y digitalizar procesos, incrementar la integración de brokers y la autogestión.

## 2019 | product
**Lovesharing Motos**
Lanzamos el primer servicio de motosharing de Canarias.

## 2019 | tech
**Plataforma del dato**
Datawarehouse a nivel de grupo.

## 2020 | product
**El Grupo apuesta por su propio CRM (Salesforce)**
Adopción de Salesforce como CRM corporativo.

## 2021 | product | ⭐
**Hyrenting**
Tras un proceso de desarrollo en 2021 se pone oficialmente en marcha Hyrenting, la solución integral para la Gestión de Flotas basada en el entorno Microsoft. Esta herramienta permite al grupo abandonar la gestión manual para alcanzar una operativa 100% digital, garantizando un control global sobre todo el ciclo de vida del vehículo: desde la cotización y contratación hasta el mantenimiento, gestión de incidencias y remarketing.

## 2021 | product
**Lanzamiento AVIS Corporate**
Creamos de cero un MaaS para el alquiler de vehículos por horas en Canarias. A través del desarrollador World Wide Mobility se crea una aplicación Ad-hoc para mejorar la experiencia de los clientes corporativos y eliminar los tiempos de espera. AVIS Corporate es la primera plataforma de alquiler por horas disponible en Canarias que permite alquiler el vehículo sin llaves ni trámites presenciales con el personal.

## 2021 | org | ⭐
**OTS (Operaciones y Tecnologías Seguras)**
DAG transforma y evoluciona su apuesta por la Seguridad de la Información creando, en colaboración con Velorcios Group, un nuevo equipo multidisciplinar de personas enfocadas exclusivamente en mantener la confidencialidad, integridad y disponibilidad de la información del Grupo.

## 2021 | tech
**Autostore**
DAG automatiza su Centro Logístico de Arinaga a través del uso de robots para almacenar y recuperar cubetas con productos de forma eficiente.

## 2021 | tech
**AIDA IT automatización y OpenSource**
Lanzamos una nueva iniciativa en la que apostamos por la automatización y el uso de herramientas OpenSource.

## 2023 | org | ⭐
**Certificación de modelos de gestión y seguridad**
DAG certifica sus modelos de gestión y seguridad a través de ISO (9001, 14001, 20000, 27001) y huella de carbono.

## 2024 | org
**Central de Compras**
Se lanza una nueva iniciativa: la Central de Compras de Velorcios & DAG. Su propósito es optimizar la gestión de costes operativos y disminuir la inversión necesaria para la adquisición de activos de TI para todo el Grupo.

## 2025 | org | ⭐
**Nace DAIT**
Creación de DAIT, la nueva unidad de tecnología del grupo.

## 2025 | tech | ⭐
**SIMA Suite desde Azure**
Se entrega desde soluciones Cloud públicas (Azure).

## 2025 | tech
**CPD como servicio**
Empezamos a tener el Datacenter como servicios con Velorcios.

## 2025 | org
**Nuevo plan de Contingencia DAG**
Implementación de nuevo plan de contingencia corporativo.`;

// Icon mapping based on content keywords
const ICON_MAP = {
    'desy': '🖥️',
    'linux': '🐧',
    'servidor': '🌐',
    'web': '🌐',
    'aplicación': '💻',
    'erp': '📊',
    'directorio': '📁',
    'cofi': '💰',
    'sima': '⚙️',
    'footprints': '🎫',
    'blackberry': '📱',
    'aida': '🏢',
    'cpd': '🏗️',
    'nube': '☁️',
    'cloud': '☁️',
    'azure': '☁️',
    'google': '🔍',
    'ax': '📊',
    'pok': '🖥️',
    'kiosk': '🖥️',
    'movilidad': '🚗',
    'dto': '🔄',
    'transformación': '🔄',
    'rms': '🚗',
    'mobility': '🚗',
    'moto': '🏍️',
    'dato': '📊',
    'data': '📊',
    'crm': '👥',
    'salesforce': '👥',
    'hyrenting': '🚙',
    'avis': '🚗',
    'ots': '🔒',
    'seguridad': '🔒',
    'autostore': '🤖',
    'robot': '🤖',
    'automatización': '🤖',
    'opensource': '🔓',
    'certificación': '✅',
    'iso': '✅',
    'compras': '🛒',
    'dait': '🏢',
    'contingencia': '🛡️'
};

// State
let milestones = [];
let filteredCategory = 'all';
let showOnlyImportant = false;

// ============================================
// UTILITY FUNCTIONS
// ============================================

function getIconForMilestone(title, description) {
    const text = (title + ' ' + description).toLowerCase();
    for (const [keyword, icon] of Object.entries(ICON_MAP)) {
        if (text.includes(keyword)) {
            return icon;
        }
    }
    return '📌'; // Default icon
}

function parseMarkdown(markdown) {
    const lines = markdown.trim().split('\n');
    const parsed = [];
    let currentMilestone = null;

    for (let line of lines) {
        line = line.trim();

        // Parse header: ## Year | category | ⭐
        if (line.startsWith('##')) {
            if (currentMilestone) {
                currentMilestone.description = currentMilestone.description.trim();
                parsed.push(currentMilestone);
            }

            const headerMatch = line.match(/##\s*(\d{4})\s*\|\s*(\w+)\s*(\|\s*⭐)?/);
            if (headerMatch) {
                currentMilestone = {
                    year: parseInt(headerMatch[1]),
                    category: headerMatch[2].toLowerCase(),
                    important: !!headerMatch[3],
                    title: '',
                    description: '',
                    icon: '📌'
                };
            }
        }
        // Parse title: **Title**
        else if (line.startsWith('**') && line.endsWith('**') && currentMilestone && !currentMilestone.title) {
            currentMilestone.title = line.slice(2, -2);
        }
        // Parse description
        else if (line && currentMilestone && currentMilestone.title) {
            currentMilestone.description += line + ' ';
        }
    }

    // Add last milestone
    if (currentMilestone) {
        currentMilestone.description = currentMilestone.description.trim();
        parsed.push(currentMilestone);
    }

    // Assign icons
    parsed.forEach(m => {
        m.icon = getIconForMilestone(m.title, m.description);
    });

    return parsed;
}

function exportToMarkdown(milestones) {
    let markdown = '';

    milestones.forEach(m => {
        const important = m.important ? ' | ⭐' : '';
        markdown += `## ${m.year} | ${m.category}${important}\n`;
        markdown += `**${m.title}**\n`;
        markdown += `${m.description}\n\n`;
    });

    return markdown.trim();
}

function saveToLocalStorage() {
    localStorage.setItem('dagtech_milestones', JSON.stringify(milestones));
}

function loadFromLocalStorage() {
    const stored = localStorage.getItem('dagtech_milestones');
    if (stored) {
        try {
            milestones = JSON.parse(stored);
            return true;
        } catch (e) {
            console.error('Error loading from localStorage:', e);
        }
    }
    return false;
}

// ============================================
// RENDERING FUNCTIONS
// ============================================

function getYearRange() {
    if (milestones.length === 0) return { min: 2000, max: 2025 };

    const years = milestones.map(m => m.year);
    return {
        min: Math.min(...years),
        max: Math.max(...years)
    };
}

function renderTimeline() {
    const yearRange = getYearRange();
    const years = [];

    for (let year = yearRange.min; year <= yearRange.max; year++) {
        years.push(year);
    }

    renderYears(years);
    renderMilestones(years);
    updateStats();
}

function renderYears(years) {
    const yearsContainer = document.getElementById('timelineYears');
    yearsContainer.innerHTML = '';

    years.forEach(year => {
        const yearMarker = document.createElement('div');
        yearMarker.className = 'year-marker';
        yearMarker.innerHTML = `
            <div class="year-dot"></div>
            <div class="year-label">${year}</div>
        `;
        yearsContainer.appendChild(yearMarker);
    });
}

function renderMilestones(years) {
    const milestonesContainer = document.getElementById('timelineMilestones');
    milestonesContainer.innerHTML = '';

    const yearCount = years.length;
    const timelineWidth = 3000; // Compact width
    const padding = 100; // Padding on edges
    const usableWidth = timelineWidth - (padding * 2);

    // 1. Calculate horizontal positions for all milestones first
    const positionedMilestones = [];

    milestones.forEach((milestone, index) => {
        const yearIndex = years.indexOf(milestone.year);
        if (yearIndex === -1) return;

        const yearPixelPos = padding + (yearIndex / (yearCount - 1)) * usableWidth;
        const milestonesInYear = milestones.filter(m => m.year === milestone.year);
        const indexInYear = milestonesInYear.indexOf(milestone);
        const totalInYear = milestonesInYear.length;

        // Dynamic spread range - more space when there are many events in a year
        const baseSpreadRange = 250;
        const spreadRange = totalInYear > 4 ? baseSpreadRange * 1.5 : baseSpreadRange;
        let offsetX = 0;

        if (totalInYear === 1) {
            offsetX = 40; // Single event: small offset to the right
        } else {
            // Multiple events: distribute them to the right of the year marker
            const step = spreadRange / totalInYear;
            offsetX = 25 + step * indexInYear; // Start closer, then space out
        }

        const leftPx = yearPixelPos + offsetX;

        // Compact cards - narrower to allow multi-line text
        const approxWidth = milestone.important ? 160 : 120;

        // Cards are centered with translateX(-50%), so we need to account for that
        const halfWidth = approxWidth / 2;

        positionedMilestones.push({
            data: milestone,
            index: index,
            left: leftPx,
            width: approxWidth,
            right: leftPx + halfWidth, // Right edge (centered card)
            leftEdge: leftPx - halfWidth // Left edge (centered card)
        });
    });

    // 2. Assign tracks (vertical levels) to avoid collision

    // Track systems
    const normalTrackEnds = [];
    const importantTrackEnds = [];

    // Configuration - Keep normal milestones LOW, important milestones HIGH
    const baseConnectorHeight = 30; // Very low for normal milestones
    const trackHeightStep = 150; // Very large steps to completely avoid overlaps

    // Important milestones configuration - Keep them very high
    const availableHeight = window.innerHeight - 100;
    // Define 3 different height levels for important milestones - all HIGH
    const importantHeights = [
        Math.min(availableHeight * 0.75, 550), // Very High
        Math.min(availableHeight * 0.68, 500), // High
        Math.min(availableHeight * 0.61, 450)  // Medium-High
    ];
    const importantStep = 90;

    // Sort by position to process left-to-right
    positionedMilestones.sort((a, b) => a.left - b.left);

    positionedMilestones.forEach(pm => {
        // Check visibility filters
        const shouldHide = (filteredCategory !== 'all' && filteredCategory !== pm.data.category) ||
            (showOnlyImportant && !pm.data.important);

        if (shouldHide) {
            renderMilestoneElement(pm, 0, true);
            return;
        }

        let connectorHeight;
        const cardGap = 100; // Even larger gap to completely prevent overlaps

        console.log(`Processing milestone: ${pm.data.title}, leftEdge: ${pm.leftEdge}, right: ${pm.right}, width: ${pm.width}`);

        if (pm.data.important) {
            // Important milestones: Use varied heights for visual interest
            // Cycle through the 3 height levels based on track index
            let trackIndex = 0;
            let placed = false;

            while (!placed) {
                if (importantTrackEnds[trackIndex] === undefined) {
                    importantTrackEnds[trackIndex] = -Infinity;
                }

                console.log(`  Important - Track ${trackIndex}: trackEnd=${importantTrackEnds[trackIndex]}, checking if ${pm.leftEdge} > ${importantTrackEnds[trackIndex] + cardGap}`);

                if (pm.leftEdge > importantTrackEnds[trackIndex] + cardGap) {
                    importantTrackEnds[trackIndex] = pm.right;
                    placed = true;
                    console.log(`  ✓ Placed on track ${trackIndex}, height level ${trackIndex % importantHeights.length}`);
                } else {
                    trackIndex++;
                }
            }

            // Each track gets a unique height - no cycling!
            // Start high and go down with each track
            connectorHeight = Math.min(availableHeight * 0.75, 550) - (trackIndex * 80);

        } else {
            // Normal milestones: Standard bottom-up tracks
            let trackIndex = 0;
            let placed = false;

            while (!placed) {
                if (normalTrackEnds[trackIndex] === undefined) {
                    normalTrackEnds[trackIndex] = -Infinity;
                }

                console.log(`  Normal - Track ${trackIndex}: trackEnd=${normalTrackEnds[trackIndex]}, checking if ${pm.leftEdge} > ${normalTrackEnds[trackIndex] + cardGap}`);

                if (pm.leftEdge > normalTrackEnds[trackIndex] + cardGap) {
                    normalTrackEnds[trackIndex] = pm.right;
                    placed = true;
                    console.log(`  ✓ Placed on track ${trackIndex}, height=${baseConnectorHeight + (trackIndex * trackHeightStep)}px`);
                } else {
                    trackIndex++;
                }
            }

            // Calculate height based on track - but cap at 300px max
            connectorHeight = Math.min(baseConnectorHeight + (trackIndex * trackHeightStep), 300);
        }

        console.log(`  Final height: ${connectorHeight}px`);

        renderMilestoneElement(pm, connectorHeight, false);
    });

    function renderMilestoneElement(pm, height, hidden) {
        const milestone = pm.data;
        const index = pm.index;

        const milestoneEl = document.createElement('div');
        milestoneEl.className = `milestone ${milestone.important ? 'important' : ''}`;
        if (hidden) milestoneEl.classList.add('hidden');

        milestoneEl.setAttribute('data-category', milestone.category);
        milestoneEl.style.cssText = `
            left: ${pm.left}px;
            z-index: ${milestone.important ? 100 : 10 + Math.floor(height / 10)}; /* Important always on top */
        `;

        milestoneEl.innerHTML = `
            <div class="milestone-connector" style="height: ${height}px;"></div>
            <div class="milestone-card">
                <div class="milestone-star ${milestone.important ? 'active' : ''}" data-index="${index}">⭐</div>
                <div class="milestone-row">
                    <span class="milestone-title">${milestone.title}</span>
                </div>
            </div>
        `;

        // Add hover tooltip
        milestoneEl.addEventListener('mouseenter', (e) => showTooltip(e, milestone));
        milestoneEl.addEventListener('mouseleave', hideTooltip);

        // Add star click handler
        const star = milestoneEl.querySelector('.milestone-star');
        star.addEventListener('click', (e) => {
            e.stopPropagation();
            toggleImportant(index);
        });

        milestonesContainer.appendChild(milestoneEl);
    }
}

function randomBetween(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}

// ============================================
// TOOLTIP
// ============================================

function showTooltip(event, milestone) {
    const tooltip = document.getElementById('tooltip');
    const tooltipContent = tooltip.querySelector('.tooltip-content');

    tooltipContent.innerHTML = `
        <strong style="font-size: 1.1rem; display: block; margin-bottom: 0.5rem;">${milestone.title}</strong>
        <div style="color: var(--text-secondary); margin-bottom: 0.75rem;">${milestone.year} · ${getCategoryName(milestone.category)}</div>
        <div>${milestone.description}</div>
    `;

    tooltip.classList.add('visible');
    positionTooltip(event, tooltip);
}

function positionTooltip(event, tooltip) {
    const x = event.clientX;
    const y = event.clientY;
    const offset = 20;

    tooltip.style.left = `${x + offset}px`;
    tooltip.style.top = `${y + offset}px`;
}

function hideTooltip() {
    const tooltip = document.getElementById('tooltip');
    tooltip.classList.remove('visible');
}

function getCategoryName(category) {
    const names = {
        'aida': 'AIDA',
        'dto': 'DTO',
        'negocio': 'Negocio'
    };
    return names[category] || category;
}

// ============================================
// ACTIONS
// ============================================

function toggleImportant(index) {
    milestones[index].important = !milestones[index].important;
    saveToLocalStorage();
    renderTimeline();
}

function applyFilters() {
    renderTimeline();
}

function importFromMarkdown() {
    const textarea = document.getElementById('markdownInput');
    const markdown = textarea.value.trim();

    if (!markdown) {
        alert('Por favor, introduce datos en formato markdown');
        return;
    }

    try {
        const parsed = parseMarkdown(markdown);
        if (parsed.length === 0) {
            alert('No se encontraron hitos válidos en el markdown');
            return;
        }

        milestones = parsed;
        saveToLocalStorage();
        renderTimeline();
        closeEditor();
        alert(`✅ Se importaron ${parsed.length} hitos correctamente`);
    } catch (e) {
        alert('Error al parsear el markdown: ' + e.message);
        console.error(e);
    }
}

function exportMarkdownToTextarea() {
    const textarea = document.getElementById('markdownInput');
    const markdown = exportToMarkdown(milestones);
    textarea.value = markdown;

    // Copy to clipboard
    textarea.select();
    document.execCommand('copy');
    alert('✅ Markdown exportado y copiado al portapapeles');
}

function resetToInitialData() {
    if (confirm('¿Estás seguro de restaurar los datos iniciales? Se perderán los cambios actuales.')) {
        milestones = parseMarkdown(INITIAL_DATA);
        saveToLocalStorage();
        renderTimeline();
        alert('✅ Datos restaurados a la versión inicial');
    }
}

function clearAllData() {
    if (confirm('⚠️ ¿Estás seguro de eliminar TODOS los hitos? Esta acción no se puede deshacer.')) {
        milestones = [];
        saveToLocalStorage();
        renderTimeline();
        alert('✅ Todos los datos han sido eliminados');
    }
}

function updateStats() {
    const total = milestones.length;
    const important = milestones.filter(m => m.important).length;
    const yearRange = getYearRange();

    document.getElementById('totalMilestones').textContent = total;
    document.getElementById('importantMilestones').textContent = important;
    document.getElementById('yearRange').textContent = `${yearRange.min}-${yearRange.max}`;
}

// ============================================
// EDITOR PANEL
// ============================================

function openEditor() {
    document.getElementById('editorPanel').classList.add('open');
    document.getElementById('overlay').classList.add('visible');
}

function closeEditor() {
    document.getElementById('editorPanel').classList.remove('open');
    document.getElementById('overlay').classList.remove('visible');
}

// ============================================
// EVENT LISTENERS
// ============================================

document.addEventListener('DOMContentLoaded', () => {
    // Load data
    if (!loadFromLocalStorage()) {
        milestones = parseMarkdown(INITIAL_DATA);
        saveToLocalStorage();
    }

    // Initial render
    renderTimeline();

    // Header buttons
    document.getElementById('openEditorBtn').addEventListener('click', openEditor);
    document.getElementById('closeEditorBtn').addEventListener('click', closeEditor);
    document.getElementById('overlay').addEventListener('click', closeEditor);

    // Toggle important filter
    document.getElementById('toggleImportantBtn').addEventListener('click', (e) => {
        showOnlyImportant = !showOnlyImportant;
        e.target.classList.toggle('active');
        applyFilters();
    });

    // Category filters
    document.querySelectorAll('.filter-btn').forEach(btn => {
        btn.addEventListener('click', (e) => {
            document.querySelectorAll('.filter-btn').forEach(b => b.classList.remove('active'));
            e.target.classList.add('active');
            filteredCategory = e.target.getAttribute('data-category');
            applyFilters();
        });
    });

    // Menu functionality
    const navMenu = document.getElementById('navMenu');
    const menuBtn = document.getElementById('menuBtn');
    const closeMenuBtn = document.getElementById('closeMenuBtn');

    function toggleMenu() {
        navMenu.classList.toggle('open');
    }

    menuBtn.addEventListener('click', toggleMenu);
    closeMenuBtn.addEventListener('click', toggleMenu);

    // Close menu when clicking outside content
    navMenu.addEventListener('click', (e) => {
        if (e.target === navMenu) {
            toggleMenu();
        }
    });

    // Editor actions
    document.getElementById('importMarkdownBtn').addEventListener('click', importFromMarkdown);
    document.getElementById('exportMarkdownBtn').addEventListener('click', exportMarkdownToTextarea);
    document.getElementById('resetDataBtn').addEventListener('click', resetToInitialData);
    document.getElementById('clearDataBtn').addEventListener('click', clearAllData);

    // Version switcher
    document.getElementById('switchVersionBtn').addEventListener('click', () => {
        window.location.href = 'index.html';
    });
});
